# Advent of Code 2024

Days written in Rust require [rustup](https://rustup.rs/) to compile.

To run a given day N:

```console
$ cargo run --bin day-N
```

To run all tests:

```console
$ cargo test
```
